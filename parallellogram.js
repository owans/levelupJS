const Calculator ={
    areaOfParalellogram:function(base , height){
        
        const area = (6 * 5);
        return area.toFixed(decimalPlace);
    }
    
}

console.log(Calculator.areaOfParalellogram);